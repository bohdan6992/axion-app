<Window x:Class="Axion.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AxiON"
        Height="780" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#030303"
        Foreground="#D4D4D8"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- ===== Deep Space Glass Terminal Palette ===== -->
        <SolidColorBrush x:Key="Bg" Color="#030303"/>
        <SolidColorBrush x:Key="Panel" Color="#0A0A0A" Opacity="0.6"/>
        <SolidColorBrush x:Key="Panel2" Color="#050505" Opacity="0.9"/>
        <SolidColorBrush x:Key="BorderBrush1" Color="White" Opacity="0.06"/>

        <SolidColorBrush x:Key="Text" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="MutedText" Color="#71717A"/>

        <!-- Updated Colors -->
        <SolidColorBrush x:Key="Lavender" Color="#C084FC"/>
        <SolidColorBrush x:Key="AccentCyan" Color="#22D3EE"/>
        <SolidColorBrush x:Key="GoodGreen" Color="#10B981"/>
        <SolidColorBrush x:Key="DangerRed" Color="#F43F5E"/>
        <SolidColorBrush x:Key="AccentAmber" Color="#F59E0B"/>
        <SolidColorBrush x:Key="InfoViolet" Color="#8B5CF6"/>

        <!-- ===== Fonts ===== -->
        <FontFamily x:Key="MonoFont">Consolas, Monospace</FontFamily>
        
        <!-- ===== Buttons Style ===== -->
        <Style x:Key="BtnBase" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Text}" />
            <Setter Property="Background" Value="{StaticResource Panel2}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimaryGreen" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="#064E3B" />
            <Setter Property="BorderBrush" Value="#10B981" />
            <Setter Property="Foreground" Value="#10B981" />
        </Style>

        <Style x:Key="BtnDangerRed" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="#4C0519" />
            <Setter Property="BorderBrush" Value="#F43F5E" />
            <Setter Property="Foreground" Value="#F43F5E" />
        </Style>

        <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}"/>
            <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
        </Style>

        <!-- ===== Inputs ===== -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#050505" />
            <Setter Property="Foreground" Value="{StaticResource Text}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#050505" />
            <Setter Property="Foreground" Value="{StaticResource Text}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource MutedText}"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseTitleBtn" TargetType="Button" BasedOn="{StaticResource TitleBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E11D48"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" Background="{StaticResource Bg}" Margin="10" CornerRadius="16">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===================== TITLE BAR ===================== -->
            <Grid Grid.Row="0" Background="#050505">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <Border Width="8" Height="8" CornerRadius="4" Background="{StaticResource Lavender}" Margin="0,0,10,0"/>
                        <TextBlock FontSize="12" FontWeight="Bold" FontFamily="{StaticResource MonoFont}">
                            <Run Text="AXI" Foreground="White"/>
                            <Run Text="ON" Foreground="{StaticResource Lavender}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource TitleBtn}" Click="Minimize_Click" Content="—"/>
                    <Button Style="{StaticResource TitleBtn}" Click="Maximize_Click" Content="▢"/>
                    <Button Style="{StaticResource CloseTitleBtn}" Click="Close_Click" Content="✕"/>
                </StackPanel>
            </Grid>

            <!-- ===================== MAIN CONTENT ===================== -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock FontSize="34" FontWeight="Bold">
                            <Run Text="Axi" Foreground="White"/>
                            <Run Text="ON" Foreground="{StaticResource Lavender}"/>
                        </TextBlock>
                        <TextBlock Foreground="{StaticResource MutedText}" Margin="2,4,0,0" FontFamily="{StaticResource MonoFont}" FontSize="10"
                                   Text="LAUNCHER • LOCAL_API • GITHUB_SIGNALS"/>
                    </StackPanel>

                    <!-- Status pill -->
                    <Border Grid.Column="1" Background="#0A0A0A" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="20" Padding="14,6" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse x:Name="ApiDot" Width="8" Height="8" Fill="{StaticResource DangerRed}" Margin="0,0,10,0"/>
                            <TextBlock x:Name="StatusText" Text="STOPPED" Foreground="White" FontFamily="{StaticResource MonoFont}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- NEW LAYOUT GRID -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="420"/> <!-- Left Column -->
                        <ColumnDefinition Width="24"/>  <!-- Gap -->
                        <ColumnDefinition Width="*"/>   <!-- Right Column -->
                    </Grid.ColumnDefinitions>

                    <!-- LEFT COLUMN: Controls & Telemetry -->
                    <StackPanel Grid.Column="0">
                        <!-- Controls Card -->
                        <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="CONTROLS" FontSize="10" FontFamily="{StaticResource MonoFont}" FontWeight="Bold" Foreground="{StaticResource MutedText}" Margin="0,0,0,15"/>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Style="{StaticResource BtnPrimaryGreen}" Height="52" Click="StartApi_Click" Content="▶  START_API"/>
                                    <Button Grid.Column="2" Style="{StaticResource BtnDangerRed}" Height="52" Click="StopApi_Click" Content="■  STOP_API"/>
                                </Grid>
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Style="{StaticResource BtnGhost}" Height="46" Click="OpenSwagger_Click" Content="↗  OPEN_DOCS"/>
                                    <Grid Grid.Column="2">
                                        <Button x:Name="UpdateAppBtn" Style="{StaticResource BtnGhost}" Height="46" Click="UpdateApp_Click" Content="⬇  CHECK_UPD"/>
                                        <Ellipse x:Name="UpdateDot" Width="8" Height="8" Fill="{StaticResource AccentAmber}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-2,-2,0" Visibility="Collapsed"/>
                                    </Grid>
                                </Grid>
                                <TextBlock x:Name="UpdateText" Text="SYSTEM_SYNCING..." FontFamily="{StaticResource MonoFont}" FontSize="10" Foreground="{StaticResource MutedText}"/>
                            </StackPanel>
                        </Border>

                        <!-- Telemetry Card -->
                        <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="16" Padding="20">
                            <StackPanel>
                                <TextBlock Text="TELEMETRY" FontSize="10" FontFamily="{StaticResource MonoFont}" FontWeight="Bold" Foreground="{StaticResource MutedText}" Margin="0,0,0,15"/>
                                <UniformGrid Columns="1">
                                    <Border Background="#050505" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="API" FontSize="9" FontFamily="{StaticResource MonoFont}" Foreground="{StaticResource MutedText}"/>
                                            <TextBlock x:Name="ApiText" Text="API: STOPPED" Margin="0,4,0,0" FontFamily="{StaticResource MonoFont}" FontSize="12" Foreground="White" FontWeight="Bold"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#050505" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="VERSION" FontSize="9" FontFamily="{StaticResource MonoFont}" Foreground="{StaticResource MutedText}"/>
                                            <TextBlock x:Name="VersionText" Text="v0.0.0" Margin="0,4,0,0" FontFamily="{StaticResource MonoFont}" FontSize="12" Foreground="White" FontWeight="Bold"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#050505" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="SIGNALS" FontSize="9" FontFamily="{StaticResource MonoFont}" Foreground="{StaticResource MutedText}"/>
                                            <TextBlock x:Name="SignalsText" Text="WAITING" Margin="0,4,0,0" FontFamily="{StaticResource MonoFont}" FontSize="11" Foreground="{StaticResource InfoViolet}"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#050505" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="12" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="INTEGRITY" FontSize="9" FontFamily="{StaticResource MonoFont}" Foreground="{StaticResource MutedText}"/>
                                            <TextBlock x:Name="UpdatesSummaryText" Text="VERIFIED" Margin="0,4,0,0" FontFamily="{StaticResource MonoFont}" FontSize="11" Foreground="{StaticResource GoodGreen}"/>
                                        </StackPanel>
                                    </Border>
                                </UniformGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- RIGHT COLUMN: Auth & Log -->
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Auth Portal -->
                            <RowDefinition Height="*"/>    <!-- Log -->
                        </Grid.RowDefinitions>

                        <!-- Auth Portal Card -->
                        <Border Grid.Row="0" Background="{StaticResource Panel}" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="AUTH_PORTAL" FontSize="10" FontFamily="{StaticResource MonoFont}" FontWeight="Bold" Foreground="{StaticResource Lavender}" Margin="0,0,0,15"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="EmailBox" Grid.Column="0" Height="42" VerticalContentAlignment="Center"/>
                                    <PasswordBox x:Name="PasswordBox" Grid.Column="2" Height="42" VerticalContentAlignment="Center"/>
                                    <Button Grid.Column="4" Style="{StaticResource BtnGhost}" Height="42" Width="180" Click="Login_Click" Content="ESTABLISH_SESSION"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- System Log Card -->
                        <Border Grid.Row="1" Background="{StaticResource Panel2}" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="16" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" FontSize="10" FontFamily="{StaticResource MonoFont}" FontWeight="Bold">
                                    <Run Text=">_ SYSTEM_LOG" Foreground="{StaticResource Lavender}"/>
                                    <Run Text=" (LIVE_FEED)" Foreground="{StaticResource MutedText}"/>
                                </TextBlock>
                                <TextBox Grid.Row="1" x:Name="LogBox" Margin="0,12,0,0" IsReadOnly="True" BorderThickness="0" Background="Transparent" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontSize="12" Opacity="0.7"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>